import {
  colorTokens,
  radiusTokens,
  spacingTokens,
  typographyTokens,
} from '@pokemongonexus/shared-ui-tokens';

export const theme = {
  colors: colorTokens,
  radius: radiusTokens,
  spacing: spacingTokens,
  type: typographyTokens,
} as const;

